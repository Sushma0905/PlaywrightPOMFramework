import { test, expect } from '@playwright/test';
test('Verify that a user can log out successfully', async ({ page }) => {
  // Navigate to the login page
  console.log("Logout successfully");   })
  